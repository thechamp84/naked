import { ErrorDialogPromise } from './ErrorDialog';
import { SuccessDialogPromise } from './SuccessDialog';

export const DialogToast = {
    success: SuccessDialogPromise,
    error: ErrorDialogPromise,
};
